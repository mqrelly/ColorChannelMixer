COlorChannelMixer TODO
======================

- Add the possibility to filter one source (extract a color channel).
- Add destination filename pattern (read only?) after the source panels.
- Add a label showing the current dest file in processing.
- Remember last used settings.
- Try the lock-and-copy method (if it's faster).
- Set result image's dpi according to the dpi of the sources.
- Add custom color mixing.
- Make the prog process CLI args without forms.
